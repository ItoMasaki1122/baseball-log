
  


<% if users.any? %>
  <ul class="list-unstyled">
    <% users.each do |user| %>
      <li class="media">
        <img class="mr-2 rounded" src="<%= gravatar_url(user, { size: 80 }) %>" alt="">
        <div class="media-body">
          <div>
            <%= user.name %> お気に入りチーム：<%= user.likes.first.name %>
          </div>
          <div>
            <%= user.introduce %>
          </div>
          <div>
            <p><%= link_to 'View profile', user_path(user) %></p>
          </div>
        </div>
      </li>
    <% end %>
  </ul>
  <%= paginate users %>
<% end %>
